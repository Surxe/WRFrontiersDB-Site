---
export interface Props {
  parseObjectUrl: string;
  id: string;
  myVersions: string[];
  versionsData: Record<string, any>;
}

const { parseObjectUrl, id, myVersions, versionsData } = Astro.props;

// Generate the base URL based on parseObjectUrl
const baseUrl = `/WRFrontiersDB-Site/${parseObjectUrl}/${id}`;
---

<h2>Available Versions</h2>
<ul>
  {myVersions.map((version: string) => (
    <li>
      <a href={`${baseUrl}/${version}`}>
        {versionsData[version]?.title || version} ({version})
      </a>
    </li>
  ))}
</ul>